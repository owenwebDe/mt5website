# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Python Bridge Configuration
PYTHON_PORT=5001
PYTHON_BRIDGE_URL=http://localhost:5001
ENABLE_PYTHON_BRIDGE=true

# MongoDB Configuration (Optional - leave empty to run without database)
MONGODB_URI=mongodb://localhost:27017/mt5_trading

# MT5 Configuration (Optional - can be configured via frontend login)
MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=
